export type socialsProps = {
  icon: string;
  link: string;
};

export type navLinksProps = {
  text: string;
  link: string;
};

export type gridProp = {
  id: number;
  title: string;
  category: string;
  picks: number;
  price: string;
  image: string;
};
